<div id="ticket-show" data-ticket-id="<%= @ticket.id %>">

  <% if @ticket.status == 'En attente' %>
    <div class="container ticket-response p-t">
      <div class="content-t">
        <h1>Nous recherchons un vendeur disponible pour vous aider</h1>
        <div class="loader">
          Loading...
        </div>
      </div>
      <br>
      <div class="timer">
        <span id="time">02:00 </span>
        <script type="text/javascript">
          function startTimer(duration, display) {
            var timer = duration, minutes, seconds;
            setInterval(function () {
              minutes = parseInt(timer / 60, 10);
              seconds = parseInt(timer % 60, 10);
              minutes = minutes < 10 ? "0" + minutes : minutes;
              seconds = seconds < 10 ? "0" + seconds : seconds;
              display.textContent = minutes + ":" + seconds;

              if (--timer <= 0) {
                document.getElementById('time').innerHTML = '<a href="tel:+33645517408">Appeler le Callcenter</a>';
              }
            }, 1000);
          }

          window.onload = function () {
            var twoMinutes = 60 * 2,
            display = document.querySelector('#time');
            startTimer(twoMinutes, display);
          };
        </script>
      </div>
      <div style="padding-top: 5vh">
        <%= link_to 'Annuler', ticket_path(@ticket), method: :delete, :class => 'btn btn-primary' %>
      </div>
    </div>

  <% elsif @ticket.status == 'En cours' %>
    <div class="container ticket-response" style="padding-top: 27vh">
      <div class="content-t">
        <h2>
          Notre vendeur <span style="color:#008bd2; font-weight: 900">pédale</span> à votre rescousse vers le rayon <span style="color:#008bd2; font-weight: 900"> <%= @ticket.section %> </span>
        </h2>
        <div class="biking">
          <img src="https://media.giphy.com/media/l0IsHKjavEvd0ihG0/giphy.gif" alt="">
        </div>
      </div>
    </div>

  <% elsif @ticket.status == 'Plus tard' %>
    <div class="container ticket-response" style="padding-top: 27vh">
      <div class="content-t">
        <h2>
          Notre vendeur est en pleine <span style="color:#008bd2; font-weight: 900">étape</span> avec un autre client. Il se rendra vers vous dès qu'il arrivera au <span style="color:#008bd2; font-weight: 900">sommet</span>.
        </h2>
        <div class="biking">
          <img src="https://media.giphy.com/media/l0IsHKjavEvd0ihG0/giphy.gif" alt="">
        </div>
      </div>
    </div>

  <% else %>
    <div class="container ticket-response" style="margin-top: 10vh">
      <div class="content-t">
        <h2>Nous espérons avoir répondu à vos interrogations</h2>
        <h1 style="margin-top: 5vh">Merci</h1>
        <div class="heart" style="margin-top: 35vh"></div>
      </div>
      <div>
        <div class="review" style="margin-top: 22vh">
          <p class="desc" style="font-family: sans-serif; font-size:1.2rem">
            Merci d'évaluer le conseil de votre <span style="color:#008bd2; font-weight:bold;font-size:1.5rem;text-transform: uppercase">vendeur</span>
          </p>
          <%= simple_form_for [@ticket, @review] do |f| %>
            <%= f.input :rating, as: :hidden  %>
            <div class="my-3">
              <% 5.times do |index| %>
                <i id="<%= index + 1 %>" class="review-rating far fa-star"></i>
              <% end %>
            </div>
            <%= f.button :submit, "Valider et revenir à l'acceuil", class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
      <div>
        <a href="https://www.anphibia.com"><span class="far fa-hand-point-right" style="color: #008bd2; margin-top:5vh" > Revenez à l'acceuil directement <br>ou</span></a>
      </div>
      <div>
        <a href="https://fr.surveymonkey.com/r/QZFFVSW"><span class="fas fa-praying-hands" style="color: #008bd2; margin-top:5vh"> Donnez votre avis sur l'application !</span></a>
      </div>
      </div>
    </div>
  <% end %>

</div>


